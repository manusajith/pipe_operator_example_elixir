# PipeOperatorExample

**TODO: Add description**

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed as:

  1. Add pipe_operator_example to your list of dependencies in `mix.exs`:

        def deps do
          [{:pipe_operator_example, "~> 0.0.1"}]
        end

  2. Ensure pipe_operator_example is started before your application:

        def application do
          [applications: [:pipe_operator_example]]
        end

